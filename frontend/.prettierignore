# Build output and dependencies
build/
.svelte-kit/
dist/
node_modules/

# Generated by npm: reformatting it produces a huge, meaningless diff on every
# `npm run format`, and npm rewrites it back on the next install.
package-lock.json

# npm owns the formatting of these files (2-space indent); letting Prettier
# convert them to tabs makes every version bump look like a full rewrite.
package.json
